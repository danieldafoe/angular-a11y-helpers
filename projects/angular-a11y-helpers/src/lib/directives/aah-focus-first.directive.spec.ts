import { AahFocusFirstDirective } from './aah-focus-first.directive';

describe('AahFocusFirstDirective', () => {
  it('should create an instance', () => {
    const directive = new AahFocusFirstDirective();
    expect(directive).toBeTruthy();
  });
});
